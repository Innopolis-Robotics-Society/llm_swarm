# Записи, выведенные из набора

Лежат здесь, а не удалены: они описывают реальное поведение стека и именно на
них видно, что M3 упирается в 8/9. Из набора выведены потому, что ни одна из
них не отвечает на вопрос ячейки. `analyze_sessions.py` сюда не смотрит —
корень по умолчанию `paper/results/sessions`.

| запись | ячейка | почему выведена |
|---|---|---|
| `20260815_153035` | B-M3 rep 1 | провайдер Chutes (кампания идёт на Parasail, §4.6: смена провайдера перегоняет ячейку целиком); плюс `llm_chat/` отсутствует, счётчики сеток потеряны |
| `20260815_154541` | B-M3 rep 2 | то же: Chutes и нет `llm_chat/` |
| `20260815_160042` | B-M3 rep 3 | Chutes; плюс план отклонён исполнителем — одна параллель слала `robot_10` и `robot_11` в две разные цели, и вся ветка отброшена. Дефект исправлен в `ec688ec`, кода, который дал этот 0/9, в HEAD больше нет |

Что из них всё-таки стоит помнить: 8/9 в первых двух — это не случайность, обе
уперлись в одну и ту же задачу `carry_shields_to_reactor`, застрявшую в
`carrying`. Тот же потолок повторил и первый прогон на Parasail.
