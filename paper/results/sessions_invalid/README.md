# Записи, выведенные из набора

Лежат здесь, а не удалены. `analyze_sessions.py` сюда не смотрит — корень по
умолчанию `paper/results/sessions`.

| запись | ячейка | почему выведена |
|---|---|---|
| `20260815_160042` | B-M3 rep 3 | план отклонён исполнителем: одна параллель слала `robot_10` и `robot_11` в две разные цели, и вся ветка отброшена, отсюда 0/9 при исправном планировании. Дефект исправлен в `ec688ec` — кода, который дал этот результат, в HEAD больше нет |

Прогоны `20260815_153035` и `20260815_154541` сюда попадали и **возвращены в
набор**: см. `sessions/README_B-M3.md`.
